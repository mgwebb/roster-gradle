---

spring:
  application:
    name: roster
  cloud:
    config:
      uri: ${vcap.services.config-service.credentials.uri}

management:
  security:
    enabled: false

security:
  basic:
    enabled: false